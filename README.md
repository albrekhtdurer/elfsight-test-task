# Elfsight Test Task (Rick and Morty API)

## Запуск проекта

* Склонировать проект на ветке `main`
* Установить зависимости `npm install`
* Запустить проект командой `npm run start`. Приложение будет доступно по адресу `localhost:3000`.